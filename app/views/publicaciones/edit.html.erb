<h1>Editar publicacion</h1>

<% form_for([@docente, @publicacion]) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => { :f => f } %>

<table style="width: 900px; margin: auto; text-align: center; padding: 20px">
  <tr>
    <td>
      <%= f.submit 'Actualizar' %> <% end %> |
      <%= link_to 'Ver', docente_publicacion_path(@publicacion) %> |
      <%= link_to 'Atras', docente_publicaciones_path(@docente) %>
    </td>
  </tr>
</table>