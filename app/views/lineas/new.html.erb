<h1>Nuevo proyecto</h1>

<% form_for([@docente, @linea]) do |f| %>
  <%= f.error_messages %>
<%= render :partial => 'form', :locals => { :f => f } %>

<table style="width: 900px; margin: auto; text-align: center; padding: 20px">
  <tr>
    <td>
      <%= f.submit 'Crear' %> <% end %> |
      <%= link_to 'Atras', docente_lineas_path(@docente) %>
    </td>
  </tr>
</table>