<h1>Editar experiencia laboral</h1>

<% form_for([@docente, @experiencia]) do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :locals => { :f => f } %>

<table style="width: 900px; margin: auto; text-align: center; padding: 20px">
  <tr>
    <td>
      <%= f.submit 'Actualizar' %><% end %> |
      <%= link_to 'Ver', docente_experiencia_path(@experiencia) %> |
      <%= link_to 'Atras', docente_experiencias_path(@docente) %>
    </td>
  </tr>
</table>